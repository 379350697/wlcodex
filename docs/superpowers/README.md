# 历史设计与 review 归档

> **状态：SUPERSEDED。** `specs/`、`plans/`、`reviews/` 和 `reports/` 中所有
> 按日期命名的 Markdown 文档都是当时的设计、计划、评审或证据快照。它们**不再
> 是 WLCodex 的现行产品事实或发布合同**，也不能作为新增功能或回归判断的依据。

现行事实只以以下文档和运行代码/测试为准：

- [当前产品语义合同](../product-semantics.md)
- [Raw frame 维护手册](../operations/raw-frame-retention.md)
- 根目录 [README](../../README.md)

历史材料保留用于解释迁移和审计上下文。阅读其中的 Workbench、Cockpit、Onsite、
Telegram 主入口、旧执行模式或旧 UI 结论时，均须将其视为 superseded。
